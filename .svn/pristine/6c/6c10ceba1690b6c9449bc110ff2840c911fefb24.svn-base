import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MineFairnessComponent } from './fairness.component';

describe('ModalMineFairnessComponent', () => {
  let component: MineFairnessComponent;
  let fixture: ComponentFixture<MineFairnessComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MineFairnessComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MineFairnessComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
