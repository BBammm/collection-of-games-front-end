import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LadderFairnessComponent } from './fairness.component';

describe('ModalLadderFairnessComponent', () => {
  let component: LadderFairnessComponent;
  let fixture: ComponentFixture<LadderFairnessComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LadderFairnessComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LadderFairnessComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
