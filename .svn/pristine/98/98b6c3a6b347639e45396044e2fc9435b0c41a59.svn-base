import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ModalGameHistoryComponent } from './modal-game-history.component';

describe('ModalGameHistoryComponent', () => {
  let component: ModalGameHistoryComponent;
  let fixture: ComponentFixture<ModalGameHistoryComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ModalGameHistoryComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ModalGameHistoryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
