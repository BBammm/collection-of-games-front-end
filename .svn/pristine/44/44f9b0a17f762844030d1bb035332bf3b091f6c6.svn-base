import { TestBed } from '@angular/core/testing';

import { AuthSigninRequiredGuard } from './auth-signin-required.guard';

describe('AuthSigninRequiredGuard', () => {
  let guard: AuthSigninRequiredGuard;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    guard = TestBed.inject(AuthSigninRequiredGuard);
  });

  it('should be created', () => {
    expect(guard).toBeTruthy();
  });
});
